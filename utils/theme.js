const config = {
    initialColorMode: 'dark',
};

const components = {
    Container: {
        baseStyle: {
            maxW: 'container.xl',
        },
    },
};

const theme = { config, components };

export default theme;
